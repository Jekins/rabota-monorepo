import styled from 'styled-components'
import { sv } from '@rabota/utils/helpers/styles/variables'

const Block = styled.div`
  margin-top: ${sv.spacing.large};
  margin-bottom: ${sv.spacing.small};
`

export default { Block }
